package com.example.defi.defi.repository;

import com.example.defi.defi.model.UniFeeByChain;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UniFeeByChainRepository extends JpaRepository<UniFeeByChain, Long> {
}
